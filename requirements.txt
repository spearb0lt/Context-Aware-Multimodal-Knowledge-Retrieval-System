# =====================================================
# Context-Aware Multimodal Knowledge Retrieval System
# Requirements
# =====================================================

# --- Streamlit app ---
streamlit>=1.35.0

# --- PDF Parsing & Element Extraction ---
docling>=2.0.0               # IBM Docling — primary PDF extractor (text, tables, images)
pdfplumber>=0.11.0
pymupdf>=1.24.0          # fitz - fast PDF reading & metadata
pillow>=10.0.0
lxml>=5.0.0

# --- OCR (optional — install one or both) ---
pytesseract>=0.3.10          # OCR option A — requires Tesseract system binary (see README)
easyocr>=1.7.0               # OCR option B — pure Python deep-learning OCR, no system binary

# --- LangChain Core ---
langchain>=1.3.0
langchain-classic>=1.0.0      # backward-compat: InMemoryStore, MultiVectorRetriever
langchain-core>=1.0.0
langchain-community>=0.4.0
langchain-chroma>=0.1.0       # dedicated ChromaDB integration

# --- LLM Integrations ---
langchain-google-genai>=2.0.0   # Gemini (vision + text)
langchain-groq>=0.2.0            # Groq (fast LLM for text/tables)
langchain-huggingface>=0.1.0     # HuggingFace embeddings

# --- Google Generative AI ---
google-generativeai>=0.8.0

# --- Embeddings ---
sentence-transformers>=3.0.0

# --- Vector Store ---
chromadb>=0.5.0

# --- Tokenization ---
tiktoken>=0.7.0

# --- Data & Utilities ---
pandas>=2.0.0
numpy>=1.26.0
python-dotenv>=1.0.0
opencv-python-headless>=4.9.0
matplotlib>=3.8.0
ipywidgets>=8.0.0
tqdm>=4.66.0

# --- CLIP visual embeddings ---
open-clip-torch>=2.24.0

# --- Persistent disk cache ---
diskcache>=5.6.0

# --- Markdown rendering ---
markdown>=3.5.0
tabulate>=0.9.0              # required for df.to_markdown()

# --- Jupyter (notebook work) ---
ipykernel>=6.29.0
ipython>=8.0.0
jupyter>=1.0.0
notebook>=7.0.0
